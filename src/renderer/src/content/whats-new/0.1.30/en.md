## Workflows

- **Sudo-aware Git tags:** the live tags dropdown now uses the connection's **Sudo password** (when set) for repositories only accessible as another user, and real error messages (permission denied, old Git on the server) now show up instead of a generic "no tags found"

See the [Workflows](workflows) chapter in the manual.
