## Workflows

- **Tags Git com sudo:** o dropdown ao vivo de tags agora usa a **Senha sudo** da conexão (quando cadastrada) pra repositórios só acessíveis como outro usuário, e mensagens de erro reais (permissão negada, Git antigo no servidor) aparecem no lugar do "nenhuma tag encontrada" genérico

Veja o capítulo [Workflows](workflows) no manual.
