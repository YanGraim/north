## Workflows

- **Workflow run** — the run tab identifies **client** and environment (**HML** / **Prod**), like the session.
- **Workflow log** — follows the step output; scrolling up pauses follow.

See [Workflows](workflows).
