## API client

- **Format JSON:** with a JSON body, the **Format** button re-indents pasted or typed content
- **Select snippets of the response:** the response body (and the request body) now supports text selection with the mouse — no more copying the whole response just to grab a piece of it

See the [API client](api) manual chapter.
