## Workflows

- **Authenticated Git tags:** the live tags dropdown now uses the connection's **Git username** and **Git password** when an HTTPS remote asks for credentials — the same secrets deploy steps already used

See the [Workflows](workflows) chapter in the manual.
